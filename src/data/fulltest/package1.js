import { listeningTest } from '../listening/package1'
import { structureTest } from '../structure/package1'
import { readingSections } from '../reading/package1'

export const fullTest = {
  listening: listeningTest,
  structure: structureTest,
  reading: readingSections
}