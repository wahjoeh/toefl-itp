import { listeningTest } from '../listening/package2'
import { structureTest } from '../structure/package2'
import { readingSections } from '../reading/package2'

export const fullTest = {
  listening: listeningTest,
  structure: structureTest,
  reading: readingSections
}